<h2>Welcome <%=@user.first_name%></h2>

<% @user.posts.each do |post| %>
<!-- add a partial to render this -->
  <div><a href="<%=user_post_path(@author.id,post.id)%>"><%= post.title %></a>
       <%= post.created_at.asctime %>
  </div>
<%end%>
<br>

<!-- you can just pass in @user and rails will grab the ID out of it -->
<a href="<%=new_user_post_path(@user.id)%>">New Post</a>
<br>
<a href="<%=logout_path%>">Log Out</a>
<br>
<a href="<%=root_path%>">Home Page</a>
